---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "scaling_escalation_policy Resource - scaling"
subcategory: ""
description: |-
  Manages an escalation policy with ordered steps.
---

# scaling_escalation_policy (Resource)

Manages an escalation policy with ordered steps.

## Example Usage

```terraform
resource "scaling_oncall_schedule" "primary" {
  name     = "Primary On-Call"
  timezone = "America/New_York"

  layer {
    name                 = "Engineers"
    rotation_type        = "weekly"
    rotation_length_days = 7
    handoff_time         = "09:00"
    effective_from       = "2025-01-01T00:00:00Z"
    participant_ids      = ["user_abc", "user_def"]
  }
}

resource "scaling_oncall_schedule" "management" {
  name     = "Management"
  timezone = "America/New_York"

  layer {
    name                 = "Managers"
    rotation_type        = "weekly"
    rotation_length_days = 7
    handoff_time         = "09:00"
    effective_from       = "2025-01-01T00:00:00Z"
    participant_ids      = ["user_ghi", "user_jkl"]
  }
}

# Steps escalate in the order written. Each step pages its target, then waits
# escalate_after_seconds before escalating to the next step.
resource "scaling_escalation_policy" "default" {
  name        = "Default Escalation"
  description = "Page the primary rotation, then management after 5 minutes"

  step {
    target_type            = "schedule"
    target_id              = scaling_oncall_schedule.primary.id
    escalate_after_seconds = 300
  }

  step {
    target_type            = "schedule"
    target_id              = scaling_oncall_schedule.management.id
    escalate_after_seconds = 600
  }
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) Display name of the escalation policy.

### Optional

- `description` (String) Human-readable description.
- `step` (Block List) Ordered escalation steps. Position is determined by list order. (see [below for nested schema](#nestedblock--step))

### Read-Only

- `created_at` (String) ISO 8601 creation timestamp.
- `id` (String) Unique identifier for the escalation policy.
- `org_id` (String) Organization that owns this policy.
- `updated_at` (String) ISO 8601 last-update timestamp.

<a id="nestedblock--step"></a>
### Nested Schema for `step`

Required:

- `escalate_after_seconds` (Number) Seconds to wait before escalating to next step. Minimum 60.
- `target_id` (String) ID of the target on-call schedule.
- `target_type` (String) Type of the escalation target.

Read-Only:

- `created_at` (String)
- `id` (String) Unique identifier for the step.
- `position` (Number) 1-based position, computed from block order.
- `updated_at` (String)
